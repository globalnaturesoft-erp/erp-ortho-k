<li class="nav-item <%= "active open" if ["erp/contacts/backend/contacts",
                                          "erp/ortho_k/backend/patient_states"].include?(params[:controller]) %>"">
    <a href="javascript:;" class="nav-link nav-toggle">
        <i class="icon-users"></i>
        <span class="title"><%= t('.contact') %></span>
        <% if ["erp/contacts/backend/contacts"].include?(params[:controller]) %>
            <span class="selected"></span>
            <span class="arrow open"></span>
        <% else %>
            <span class="arrow"></span>
        <% end %>
    </a>
    <ul class="sub-menu">
        <li class="nav-item <%= "active open" if ["erp/contacts/backend/contacts"].include?(params[:controller]) %>">
            <a href="<%= erp_contacts.backend_contacts_path %>" class="nav-link ">
                <span class="title"><%= t('.contacts') %></span>
                <% if ["erp/contacts/backend/contacts"].include?(params[:controller]) %>
                <span class="selected"></span>
                <% end %>
            </a>
        </li>
        <li class="nav-item <%= "active open" if ["erp/ortho_k/backend/patient_states"].include?(params[:controller]) %>">
            <a href="<%= erp_ortho_k.backend_patient_states_path %>" class="nav-link ">
                <span class="title"><%= t('.patient_states') %></span>
                <% if ["erp/ortho_k/backend/patient_states"].include?(params[:controller]) %>
                <span class="selected"></span>
                <% end %>
            </a>
        </li>
    </ul>
</li>